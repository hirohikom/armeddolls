using UnityEngine;
using System.Collections;

public class GunHanddleEx_RS : MonoBehaviour {

/*	public WeaponControllerEx_RS weapon;
	
	void Start () {
		weapon = this.transform.GetComponentInChildren (typeof(WeaponControllerEx_RS)).GetComponent<WeaponControllerEx_RS> ();
	}
	
	
	void Update () {
		if (Input.GetKey(KeyCode.E) || RepeatButton_RS.RepeatBtn) {
			WeaponControllerEx_RS.isShoot = true;
			ShootWeapon ();
		} else {
			WeaponControllerEx_RS.isShoot = false;
		}
		// You can access all weapon parameters by call GetCurrentWeapon
		// e.g. weapon.GetCurrentWeapon().Ammo.toString()
	}

	public void ShootWeapon(){
		if (weapon)
			weapon.LaunchWeapon ();
	}*/
}
