using UnityEngine;
using System.Collections;

public class GunHanddleEx_RH : MonoBehaviour {

/*	public WeaponControllerEx_RH weapon;
	
	void Start () {
		weapon = this.transform.GetComponentInChildren (typeof(WeaponControllerEx_RH)).GetComponent<WeaponControllerEx_RH> ();
	}
	
	
	void Update () {
		if (Input.GetKey(KeyCode.C)) {
			if (weapon)
				weapon.LaunchWeapon ();
		}
		// You can access all weapon parameters by call GetCurrentWeapon
		// e.g. weapon.GetCurrentWeapon().Ammo.toString()
	}*/
}
